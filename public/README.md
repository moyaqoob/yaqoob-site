# Public assets

Place **resume.pdf** here so the `/resume` page can display and offer it for download.

Copy your resume file (e.g. `yaqoob-resume (1) (1).pdf`) into this folder and rename it to `resume.pdf`.
